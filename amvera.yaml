version: null
meta:
  environment: python
  toolchain:
    name: pip
    version: latest
build:
  useCache: false   # ← ЭТО ГЛАВНОЕ! Полностью отключает кэш
run:
  command: python bot.py
serviceType: compute
